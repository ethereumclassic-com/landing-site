import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Buy Mining Hardware',
  description:
    'Where to buy GPUs and ASICs for Ethereum Classic mining.',
}

export default function BuyHardwareLayout({ children }: { children: React.ReactNode }) {
  return children
}
