import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Staking on Exchanges | Ethereum Classic',
  description: 'Understanding yield opportunities for ETC. Learn why Ethereum Classic uses Proof-of-Work and discover alternative ways to earn with your ETC holdings.',
}

export default function StakingLayout({ children }: { children: React.ReactNode }) {
  return children
}
